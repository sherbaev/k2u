#pragma once
// Copy to secrets.h (gitignored) and fill in. Do NOT commit real secrets.

#define WIFI_SSID     "your-ssid"
#define WIFI_PASSWORD "your-password"

// MQTT broker (Coolify Mosquitto). Use TLS on 8883 in production.
#define MQTT_HOST "mqtt.example.com"
#define MQTT_PORT 8883
#define MQTT_USER "K2U-01"
#define MQTT_PASS "device-password"

// Root CA (Let's Encrypt / broker CA) for WiFiClientSecure. Paste PEM here.
static const char MQTT_ROOT_CA[] = R"EOF(
-----BEGIN CERTIFICATE-----
... paste broker CA certificate ...
-----END CERTIFICATE-----
)EOF";
